storage: ./storage

auth:
  auth-memory:
    users:
      test:
        name: test
        password: test

web:
  title: verdaccio

publish:
  allow_offline: false

uplinks:

log: { type: stdout, format: pretty, level: trace }

packages:
  'hidden-*':
    access: $all
    publish: $anonymous
    visibility: test
  'guarded-*':
    access: $all
    publish: test
  '**':
    access: $all
    publish: test
_debug: true

flags:
  changePassword: true
